<?xml version="1.0" encoding="UTF-8"?>
<!--
  AutoUpdater.NET feed for BoekSolutions.SheetSetEditor.

  This file is hand-edited as part of the manual publish process described in README.md —
  never generated or pushed automatically. <version> must always match the currently
  published installer's app version (App/SheetSetEditor.App.csproj <Version> at build time),
  and <url> must point at that exact installer asset on this repo's Releases page.

  Current state: real release. v1.1.4 is published with the installer asset attached and
  verified reachable via the GitHub API (asset size and SHA256 match the locally-built installer
  byte for byte, 60763690 bytes, sha256 d63ba7ec5b246e5c49358aae174ff45e92323818edaf923b4f5d1b6003be2231)
  — installs on 1.1.3 and earlier will now be offered this update.

  Note: previous releases have had inconsistent tag casing (lowercase "v1.1.0" vs capital
  "V1.1.1") — always verify the actual tag via the GitHub API before wiring the URL below,
  don't assume the lowercase convention.

  BUG FOUND AND FIXED 2026-07-07: the installer's output filename was changed to a fixed
  "SheetSetEditor-Setup.exe" (see protect/installer/SheetSetEditor.iss, OutputBaseFilename)
  shortly after v1.1.3 was published, and the v1.1.3 release asset on GitHub was renamed to
  match — but this file's <url> was never updated, and kept pointing at the old
  "SheetSetEditor-Setup-1.1.3.exe", which no longer existed. Any user on 1.1.2 or earlier who
  triggered the update got a real 404 trying to download it. Fixed by switching to the
  version-independent "latest" permalink below, which also means this <url> never needs to
  change again on future releases (as long as OutputBaseFilename stays fixed) — only <version>
  needs updating by hand each release. The tag-casing gotcha above stops mattering for this
  URL too, since GitHub resolves "latest" itself.

  ALWAYS verify a pinned/rewritten <url> actually resolves (curl -I, expect 200 not 404) before
  treating a release as live — this exact bug shipped silently because that check was skipped
  after the asset got renamed post-publish.
-->
<item>
    <version>1.1.5</version>
    <url>https://github.com/JohnBoek/SheetSetEditor-releases/releases/latest/download/SheetSetEditor-Setup.exe</url>
    <changelog>https://github.com/JohnBoek/SheetSetEditor-releases/blob/main/CHANGELOG.md</changelog>
    <mandatory>false</mandatory>
</item>
